Decisions log for core/speech AGENTS.md:
- Decision: document voice pipeline with staged latency targets to guide implementation.
- Choice: rely on existing shared layer for config/logging instead of new abstractions.
