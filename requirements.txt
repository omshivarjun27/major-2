livekit-agents[deepgram,elevenlabs,tavus]>=1.0.0
python-dotenv>=1.0.0
librosa>=0.10.0
numpy>=1.20.0
livekit-plugins-deepgram>=1.0.0
livekit-plugins-openai>=1.0.0
livekit-plugins-silero>=1.0.0
livekit-plugins-elevenlabs>=1.0.0
livekit-plugins-tavus>=1.0.0
livekit>=1.0.0
livekit-api>=1.0.0

langchain-community
duckduckgo-search
# For Ollama API integration
ollama
# For image conversion and processing
opencv-python
Pillow>=9.0.0

# OCR backends (at least one required for text reading)
easyocr>=1.7.0
pytesseract>=0.3.10

# For local places search (OpenStreetMap Nominatim)
geopy>=2.4.0

# For local calendar (iCalendar format)
icalendar>=5.0.0

# For IMAP email reading
imapclient>=3.0.0

# =========================================================================
# QR / AR TAG SCANNING DEPENDENCIES
# =========================================================================

# QR code detection (primary backend)
pyzbar>=0.1.9

# QR code generation (for tests / demo)
qrcode[pil]>=7.4

# HTTP client for async API calls
httpx>=0.26.0

# =========================================================================
# SPATIAL PERCEPTION DEPENDENCIES
# =========================================================================

# Core ML frameworks (optional - for advanced models)
# torch>=2.0.0  # Uncomment for PyTorch-based models
# torchvision>=0.15.0  # Uncomment for vision models

# ONNX Runtime for optimized inference
onnxruntime>=1.15.0

# YOLO object detection (optional)
# ultralytics>=8.0.0  # Uncomment to enable YOLOv8

# Depth estimation (MiDaS via torch hub - requires torch)
# timm>=0.9.0  # Required for MiDaS models

# Image processing utilities
scipy>=1.10.0
scikit-image>=0.21.0

# =========================================================================
# VQA ENGINE DEPENDENCIES
# =========================================================================

# FastAPI for VQA REST API
fastapi>=0.109.0
uvicorn>=0.27.0

# Pydantic for data validation
pydantic>=2.5.0

# Testing
pytest>=7.0.0
pytest-asyncio>=0.23.0

# =========================================================================
# MEMORY ENGINE DEPENDENCIES (RAG)
# =========================================================================

# FAISS for vector similarity search (CPU version)
faiss-cpu>=1.7.4

# Sentence transformers for text embeddings
sentence-transformers>=2.2.0

# Encryption for stored data (optional)
cryptography>=41.0.0

# Anthropic SDK for Claude Opus 4.6 backend (optional)
# anthropic>=0.40.0  # Uncomment to enable Claude memory reasoning
