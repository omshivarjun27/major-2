--- a/tests/unit/test_tts_stt_edge_cases.py
+++ b/tests/unit/test_tts_stt_edge_cases.py
@@ -190,8 +190,12 @@ class TestVoiceRouterEdgeCases:
     def test_intent_type_enum_values(self):
         from core.speech.voice_router import IntentType
-        # Test expected values
-        expected = ["VISUAL", "SEARCH", "QR", "GENERAL"]
+        # Actual IntentType values (granular sub-categories, not top-level groups):
+        expected = [
+            "VISUAL_DESCRIBE", "VISUAL_IDENTIFY", "VISUAL_READ", "VISUAL_COLOR",
+            "SPATIAL_OBSTACLE", "SPATIAL_PATH", "SPATIAL_DISTANCE",
+            "QR_SCAN", "NAVIGATION_ROUTE",
+            "GENERAL_CHAT", "GENERAL_HELP", "GENERAL_COMMAND",
+            "UNKNOWN",
+        ]
         for name in expected:
             assert hasattr(IntentType, name), f"IntentType.{name} missing"

# RATIONALE: The test expected a high-level 4-value enum (VISUAL, SEARCH, QR, GENERAL)
# but the actual IntentType enum has 17 granular sub-categories. Tests should
# enumerate the actual values or test for a subset that is guaranteed to exist.
