--- a/tests/unit/test_spatial_edge_cases.py
+++ b/tests/unit/test_spatial_edge_cases.py
@@ -44,7 +44,8 @@ def _make_depth_map(fill: float = 2.5, w: int = 100, h: int = 100) -> DepthMap:
     data = np.full((h, w), fill, dtype=np.float32)
-    return DepthMap(data=data, width=w, height=h)
+    return DepthMap(depth_array=data, min_depth=float(data.min()), max_depth=float(data.max()))
 
@@ -133,30 +133,28 @@ class TestDepthMapEdgeCases:
     def test_single_pixel_depth_map(self):
         data = np.array([[3.5]], dtype=np.float32)
-        dm = DepthMap(data=data, width=1, height=1)
+        dm = DepthMap(depth_array=data)
         assert dm.depth_array[0, 0] == pytest.approx(3.5)
 
     def test_depth_map_all_zeros(self):
         data = np.zeros((100, 100), dtype=np.float32)
-        dm = DepthMap(data=data, width=100, height=100)
+        dm = DepthMap(depth_array=data)
         assert dm.depth_array.shape == (100, 100)
 
-    def test_depth_map_nan_values(self):
+    def test_depth_map_nan_values(self):  # NaN values are valid in depth arrays
         data = np.full((10, 10), np.nan, dtype=np.float32)
-        dm = DepthMap(data=data, width=10, height=10)
+        dm = DepthMap(depth_array=data)
         assert np.all(np.isnan(dm.depth_array))
 
@@ -219,10 +219,10 @@ class TestObstacleRecordEdgeCases:
     def test_all_directions_defined(self):
         expected = ["FAR_LEFT", "LEFT", "SLIGHTLY_LEFT", "CENTER",
                     "SLIGHTLY_RIGHT", "RIGHT", "FAR_RIGHT"]
-        # Note: test previously expected "AHEAD" but enum name is "CENTER" (value="ahead")
+        # Direction enum uses CENTER (not AHEAD) with value "ahead"
         for name in expected:
             assert hasattr(Direction, name), f"Direction.{name} missing"
-        # Remove erroneous "AHEAD" expectation:
-        # assert hasattr(Direction, "AHEAD")  # WRONG - enum is Direction.CENTER
 
     def test_obstacle_record_construction(self):
         record = ObstacleRecord(
             id="obs_1",
             class_name="chair",
-            direction=Direction.AHEAD,    # WRONG - use Direction.CENTER
+            direction=Direction.CENTER,
             distance_m=1.5,
             direction_deg=-12.0,
             priority=Priority.NEAR_HAZARD,
             bbox=BoundingBox(x1=0, y1=0, x2=100, y2=100),
             centroid_px=(50, 50),
         )
 
     def test_obstacle_record_very_large_distance(self):
-        record = ObstacleRecord(
-            detection=Detection(...),    # WRONG - no 'detection' field
-            ...
-        )
+        record = ObstacleRecord(
+            id="obs_far",
+            class_name="wall",
+            direction=Direction.CENTER,
+            distance_m=999.0,
+            direction_deg=0.0,
+            priority=Priority.SAFE,
+            bbox=BoundingBox(x1=0, y1=0, x2=10, y2=10),
+            centroid_px=(5, 5),
+        )
 
@@ -355,14 +355,14 @@ class TestPerceptionResultEdgeCases:
     def test_perception_result_empty(self):
         import time
         result = PerceptionResult(
             detections=[],
-            # timestamp is required since v1.1
+            timestamp=time.time(),
             image_size=(640, 480),
             latency_ms=10.0,
         )
 
# RATIONALE:
# 1. DepthMap.__init__() uses `depth_array` (not `data`/`width`/`height`) as positional fields
#    Use DepthMap.create(data=...) factory or DepthMap(depth_array=...) directly
# 2. Direction enum: AHEAD doesn't exist; use Direction.CENTER (value="ahead")
# 3. ObstacleRecord has no `detection` field; pass individual fields directly
# 4. PerceptionResult requires `timestamp: float` (added in schema v1.1)
