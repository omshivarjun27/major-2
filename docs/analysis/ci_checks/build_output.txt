Build Simulation Output
=======================
Command: pip install -e . --dry-run

Results:
  Return code: 0 (SUCCESS)
  Build backend: setuptools
  All dependencies satisfied in current venv.
  Package would install as: voice-vision-assistant==1.0.0

  No missing dependencies detected.
  No broken imports in build process.
  pyproject.toml is valid.

Docker Build Analysis (static, not executed):
  Root Dockerfile: python:3.11-slim base, valid COPY/RUN sequence
  Deployments Dockerfile: Selective COPY (only source dirs), valid

  Issues found:
  - Neither Dockerfile creates a non-root USER
  - Root Dockerfile uses COPY . . (includes .env, tests, docs)
  - .dockerignore not checked (may mitigate COPY . . issue)

import-linter:
  Not installed in current venv. Cannot verify architectural boundaries.
  CI pipeline runs it separately.
